# Сессия 1 — Основа

**Читай перед началом:** `docs/plans/00-architecture.md`, `docs/plans/02-reuse-base.md`.

## Задачи
- [ ] Довести до рабочего состояния черновик в `bot/` и `miniapp/` (config, db.py-схема, main.py: PTB + aiohttp в одном цикле). **Токена бота пока нет — поднимаем только API-часть (`miniapp/server.py` отдельно или main.py с заглушкой токена), бот подключаем, когда появится токен.**
- [ ] Дописать схему БД до полного списка из 00-АРХИТЕКТУРА (ядро елобота + лига + ставки), идемпотентно. **Ключевое:** турнир на клубах (матчи/таблица/Elo — по club_id), кошелёк/XP/ставки — на игроке (telegram_id), связь игрок↔клуб отдельной таблицей — чтобы замены не трогали балансы (принципы 0–1).
- [ ] Валидатор initData + `/api/bootstrap` и `/api/wallet` от реальной БД.
- [ ] Smoke-тест: SQLite в /tmp → все таблицы создаются; подпись initData валидируется (позитив+негатив).
- [ ] `.env.example` актуален, секреты не в git.

## Решения по бренду (2026-09-24)
Бренд клона — **KURILKA SIGARKI**. Разработка локально на SQLite, деплой — сессия 10.
Токена бота нет: `/start`-тест отложен до появления токена, критерий ниже скорректирован.

## Что уже есть (черновик от 24.09, не считать готовым)
`bot/db_backend.py` и `bot/elo.py` — скопированы из елобота; `bot/db.py`, `bot/main.py`,
`miniapp/server.py` — скелет, в тесте была ошибка итерации курсора — поправить в этой сессии.

## Критерий готовности
Локально поднимается API: `python miniapp/server.py` → `/app` отдаёт статику, `/api/bootstrap`
с валидным initData отдаёт юзера и дивизионы из SQLite; тест схемы и подписи зелёный.
(Bot-часть: `/start` пишется в `players` — проверить, когда появится токен.)
