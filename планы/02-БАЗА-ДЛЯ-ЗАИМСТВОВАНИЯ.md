# ЧТО БЕРЁМ ИЗ ЕЛОБОТА И ДРАКОБЕТА (проверено по коду)

Разведка: 2026-09-24. Пути: `~/Desktop/боты/гвардола/elobot-main`, `~/Desktop/боты/гвардола/casibet` (Дракобет), `~/Desktop/боты/гвардола/casimir` (не берём).

## ЕЛОБОТ (турнирный бот, python-telegram-bot 21.3, монолит bot.py 8.4k строк)

### Берём как есть (оттестированные самодостаточные модули) — уже скопированы в `бот/`
- ✅ `db_backend.py` (328 строк) — адаптер SQLite⇄Postgres: транслирует `?`→`%s`,
  `INSERT OR IGNORE`→`ON CONFLICT`, `AUTOINCREMENT`→`BIGSERIAL`. Бизнес-логика пишет
  чистый SQLite-диалект. Уже в `бот/db_backend.py`.
- ✅ `elo.py` (84 строки) — Elo с нулевой базой: K=32 (48 первые 10 матчей), множитель
  разницы голов 1.0–1.5, пол −500. Уже в `бот/elo.py`.
- ✅ `match_processor.py` — apply_result / revert_match / apply_walkover (664 строки):
  подтверждение матча → Elo + статистика + таблица + сетка; откат; ТП 0:3.
- ✅ `ocr.py` (2818 строк) — промпт `_AI_PROMPT` (стр. 200): JSON {score1, score2, timer,
  pen1, pen2, team1, team2, goals:[{name, minute, side}]}. Каскад провайдеров:
  OpenRouter free VL (7 моделей) → Groq → Gemini → NVIDIA NIM → OCR.space → tesseract.
  Для FC27 **переделываем промпт под новые скрины** (см. 03-УЛУЧШЕНИЯ).
- ✅ `tournament.py` (3193) — жеребьёвка групп, календарь, крест-плей-офф, серии bo1/3/5/7,
  `/simulate`. Брать частями: расписание и сетку.
- ✅ `db_export.py`, `bg_helper.py`, `emoji_helper.py`, `rich_messages.py`.
- ✅ Схема БД — источник `database.py:init_db()` (стр. 207–1306, 24 таблицы + ~40 миграций).
  Ядро переносим, см. 00-АРХИТЕКТУРА.

### Берём выборочно (переписать под наши нужды)
- Напоминания-кроны (bot.py:7020–7607): job_deadline_check (DM за 6ч + авто-ТП),
  job_reminders (15 мин, эскалация 6ч→3ч→30мин). Нам нужно 3×/день по долгам —
  шаблон job'ов берём, расписание своё.
- PNG-рендеры (standings/playoff/tour/passport image) — для постов в канал.

### Мёртвое / не брать
- ❌ корневой `leaderboard.py` (551) — легаси-дубликат, ноль вызовов (импортируется из handlers/).
- ❌ `tournament.py:circle_method_schedule()` — определена, не вызывается.
- ❌ `result.json` (2.9 МБ дамп чата), `league.db` (дев), `gavi_extract.py`, одноразовые миграции.
- ❌ ⚠️ **`Текстовий файл….txt` в корне — РЕАЛЬНЫЕ СЕКРЕТЫ** (Railway DATABASE_URL, BOT_TOKEN,
  ключи Gemini/Groq/OCR.space) закоммичены. В клон не брать, ключи оттуда — ротировать.

### Чего в елоботе НЕТ (пишем с нуля)
- Парсинг Challenge Place — ноль упоминаний.
- Трансферы (есть только `/replace_player` — замена в сетке).
- Калькулятор бюджета, долги — ноль.

## ДРАКОБЕТ (`casibet/drakobet.py`, 7.5k строк, PTB 21.7)

### Берём (это рабочий прод-движок ставок)
- ✅ Клиент SStats.net (стр. 2041–2755): `api.sstats.net` — Games/list (26 лиг),
  Odds/{game_id} (приоритет букмекеров Bet365→10Bet→Marathon→Unibet), Games/{game_id}
  (результат). **Проверено живьём 24.09.2026 — работает даже без ключа.** Ретраи на 429 есть.
- ✅ Парсер рынков `_extract_markets()`: 1X2, тоталы 1.5/2.5/3.5, BTTS, угловые 9.5,
  ЖК 4.5, двойной шанс, точный счёт, anytime scorer — почти все рынки оригинала мини-аппа.
- ✅ Атомарная ставка `save_bet()` (стр. 1399): `SELECT balance FOR UPDATE` → списание → INSERT.
- ✅ Экспресс (стр. 1686–1882): 2–10 ног, void-нога ДЕЛИТ total_odds (анти-эксплойт).
- ✅ Settle-джоба (стр. 2759–2953): раз в 2ч, не закрывает матч раньше 110 мин от старта.
- ✅ Маржа кэфов `_apply_margin()` (стр. 752) — из bot_settings.
- ✅ `db.py` (311) — второй адаптер SQLite⇄Postgres (asyncpg) — берём как образец для async.
- ✅ `db_backup.py` — ZIP+JSON бэкап/восстановление.
- ✅ Промокоды, каперы (10% с выигрыша фолловера), ивенты «угадай игрока» — по желанию.

### Не брать
- ❌ `casimir/` — не запускается (нет пакета `core/`), старая версия движка.
- ❌ Вложенные копии `casibet/casimir/`, `casibet/elobot-main/` — архив.
- ❌ Хардкоды `ADMIN_ID=6163072393`, `SSTATS_KEY` — в конфиг.

## Итог: база клона

Ставки/кэфы реального футбола НЕ нужны в мини-аппе (там виртуальная лига) — но движок
Дракобета берём для расчётной части (купоны, экспрессы, settle, лимиты), а SStats — только
если захотим отдельный бот ставок на реальный футбол рядом. Ядро же мини-аппа — кэфы на
МАТЧИ ВИРТУАЛЬНОЙ ЛИГИ, их генерим сами из силы клубов (Elo) + маржа.
