# Необязательные зависимости (в requirements.txt не входят).
# /scan по ссылке на Challenge Place через headless-браузер:
#   venv/bin/pip install -r bot/requirements-optional.txt
#   venv/bin/playwright install --with-deps chromium
playwright>=1.45
